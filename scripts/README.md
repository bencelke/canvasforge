# Scripts

Utility scripts for CanvasForge maintainers will live here in later phases.

Phase 1 intentionally keeps automation in:

- `uv` / `pyproject.toml` tool configs
- GitHub Actions CI
- `canvasforge` CLI commands

Do not add scripts that connect to Microsoft tenants, handle credentials, or download remote manifests.
